Find anagrams in anagram.py.

- [ ] In `find_anagrams` (anagram.py), return the candidates that use exactly the same letters as `word`, in their original order. Done when: `find_anagrams("listen", ["enlists", "google", "inlets", "banana"])` returns `["inlets"]`.
- [ ] In `find_anagrams` (anagram.py), compare without regard to case, but return candidates as they were given. Done when: `find_anagrams("Orchestra", ["cashregister", "Carthorse", "radishes"])` returns `["Carthorse"]`.
- [ ] In `find_anagrams` (anagram.py), a word is not its own anagram, whatever its case. Done when: `find_anagrams("BANANA", ["banana", "Banana"])` returns `[]`.
