A stack that knows its smallest item, in minstack.py.

- [ ] In `MinStack.push` and `MinStack.pop` (minstack.py), add to and take from the top; `pop` returns the item. Done when: pushing 1 then 2 and popping returns 2.
- [ ] In `MinStack.min` (minstack.py), return the smallest item currently on the stack, in constant time. Done when: after pushing 5, 3, 7 and popping once, `min()` returns 3.
- [ ] In `MinStack` (minstack.py), make `len()` work and raise `IndexError` from `pop`, `peek` and `min` on an empty stack. Done when: `MinStack().pop()` raises `IndexError`.
