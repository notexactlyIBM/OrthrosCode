Make acronyms in acronym.py.

- [ ] In `abbreviate` (acronym.py), return the first letter of each word, upper-cased. Done when: `abbreviate("Portable Network Graphics")` returns `"PNG"`.
- [ ] In `abbreviate` (acronym.py), treat hyphens as word breaks and ignore other punctuation, including underscores and apostrophes. Done when: `abbreviate("Complementary metal-oxide semiconductor")` returns `"CMOS"` and `abbreviate("The Road _Not_ Taken")` returns `"TRNT"`.
