Keep a school roster in school.py.

- [ ] In `School.add` (school.py), put a student in a grade; adding a name that is already in any grade returns False and changes nothing, otherwise returns True. Done when: adding "Aimee" to grade 2 twice returns True, then False.
- [ ] In `School.grade` (school.py), return the names in one grade, sorted. Done when: after adding "Franklin", "Bradley" and "Jeff" to grade 5, `grade(5)` returns `["Bradley", "Franklin", "Jeff"]`.
- [ ] In `School.roster` (school.py), return every name, by grade and then by name. Done when: with "Chelsea" in 3 and "Logan" in 1, `roster()` returns `["Logan", "Chelsea"]`.
