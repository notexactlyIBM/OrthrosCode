Slices of a digit string, in series.py.

- [ ] In `slices` (series.py), return every run of `length` consecutive characters, in order. Done when: `slices("49142", 3)` returns `["491", "914", "142"]`.
- [ ] In `slices` (series.py), raise `ValueError` for an empty series, a length of 0 or less, or a length longer than the series. Done when: `slices("12", 3)` raises `ValueError`.
- [ ] In `largest_product` (series.py), return the largest product of `length` consecutive digits; the product of zero digits is 1. Done when: `largest_product("63915", 3)` returns 162.
